To use this dataset, please:

1. Download `epsilon_normalized.bz2` and `epsilon_normalized.t.bz2` from
   https://www.csie.ntu.edu.tw/~cjlin/libsvmtools/datasets/binary.html#epsilon
   
2. install `gcc` and `unar`
   
3. run `./prepare.sh` (this may take several hours, depending on your CPU)